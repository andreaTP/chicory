[package]
name = "cranelift-compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
wasmtime = { version = "34", default-features = false, features = ["cranelift", "std", "all-arch"] }

[profile.release]
opt-level = "s"
lto = true
